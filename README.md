# VisionQuery
VisionQuery is a semantic image search system that uses multimodal embeddings to match natural language queries with relevant images. By using  models like CLIP, it enables zero-shot image retrieval without predefined labels, allowing users to search images using descriptive phrases instead of fixed categories.
